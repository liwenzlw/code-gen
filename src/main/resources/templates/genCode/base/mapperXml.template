<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.klxx.ta.[(${config.profile})].mapper.[(${tableMateData.className})]Mapper">

    <sql id="baseColumes">
        [# th:each="item : ${tableMateData.columns}"][(${item.dbColumnName})],
        [/]
    </sql>
</mapper>